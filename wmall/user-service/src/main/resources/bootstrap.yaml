spring:
  application:
    name: user-service
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      config:
        file-extension: yaml
        shared-configs:
          - data-id: shared-jdbc.yaml
            group: DEFAULT_GROUP
            refresh: true
          - data-id: shared-log.yaml
            group: DEFAULT_GROUP
            refresh: true
