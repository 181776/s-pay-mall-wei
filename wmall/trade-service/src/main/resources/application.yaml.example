# 复制为 application-local.yaml 并填写真实值（不会提交 Git）

server:
  port: 8085

spring:
  application:
    name: trade-service
  config:
    import: optional:classpath:application-local.yaml
  cloud:
    nacos:
      server-addr: localhost:8848

app:
  db:
    host: 127.0.0.1
    port: 13306
    username: root
    password: your-mysql-password
    database: wmall-trade
